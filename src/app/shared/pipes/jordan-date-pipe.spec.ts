import { JordanDatePipe } from './jordan-date-pipe';

describe('JordanDatePipe', () => {
  it('create an instance', () => {
    const pipe = new JordanDatePipe();
    expect(pipe).toBeTruthy();
  });
});
